[python]
# Webots utilise l'interpreteur `python` du PATH par defaut, ce qui rend ce
# projet portable d'une machine a l'autre.
#
# Le mode de perception par defaut (VISION_MODE = "color") ne demande que
# numpy, scipy, scikit-image, ikpy et torch -- ni TensorFlow ni Keras.
#
# Decommenter et adapter UNIQUEMENT si l'interpreteur par defaut n'a pas les
# dependances, ou pour reentrainer le VGG16 (VISION_MODE = "cnn") qui, lui,
# demande TensorFlow :
# COMMAND = C:\Users\<utilisateur>\anaconda3\python.exe
